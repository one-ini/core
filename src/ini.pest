ini = { SOI ~ (section | pair | comment)* ~ EOI }

section = { "[" ~ key ~ "]" }
pair = { key ~ "=" ~ value }
comment = { (";" | "#" ~ value }

key = @{ (!("]" | "=" | WHITESPACE) ~ ANY)+ }
value = @{ (!(NEWLINE) ~ ANY)+ }

WHITESPACE = _{ " " | "\t" | NEWLINE }
