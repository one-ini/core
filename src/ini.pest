ini = { SOI ~ ((pair | comment)? ~ (NEWLINE | EOI))* ~ section* }
WHITESPACE = _{ " " | "\t" }

// section
section = { "[" ~ name ~ "]" ~ (NEWLINE ~ (pair | comment)?)* }
name = @{ char+ }
char = { ASCII_ALPHANUMERIC | "." | "_" | "/" }

// key-value pair
pair = { name ~ "="? ~ value }
value = @{ char* }

comment = { indicator ~ value? }
indicator = { "#" | ";" }
